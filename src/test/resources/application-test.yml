spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false

external-service:
  mock:
    enabled: true

rate-limit:
  requests-per-minute: 100

logging:
  level:
    com.tenpo: DEBUG
